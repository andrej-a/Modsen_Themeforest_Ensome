import { valuesOfTheSettings } from '@/types/constants';

const { FAQ, PAGES, ELEMENTS, PRICING, SITE_MAP } = valuesOfTheSettings;
const services = [PAGES, ELEMENTS, FAQ, PRICING, SITE_MAP];

export default services;
